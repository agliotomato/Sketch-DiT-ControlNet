# Inverse Head 학습 아키텍처 및 전략

현재 학습 중인 **Inverse Head (역방향 스케치 추출 모델)**의 파라미터 튜닝 범위와 짝수/홀수 스텝으로 교차 진행되는 지도(Supervised) 및 순환(Cyclic) 학습의 원리를 정리한 내용입니다.

---

## 1. 모델 아키텍처: Frozen vs Tuning

거대한 생성 모델(DiT) 기반의 파이프라인 안에서 스케치를 추출하는 데 필요한 부위만 정교하게 튜닝합니다.

### Frozen
파라미터 업데이트가 발생하지 않으며, 특징(Feature) 추출 및 오차 역전파의 통로로만 사용됩니다.
* **VAE Encoder / Decoder**: 이미지 압축 및 복원 필터
* **DiT (SD3.5) 앞단 블록 (0 ~ 11)**: 기본 뼈대가 되는 거대 시각 지식
* **정방향 모델 전체 (ControlNet + DiT)**: Cycle 채점 시 스케치 기반으로 헤어를 복원하기 위해 사용됨

### Tuning
* **FPN Mask Decoder (100% 학습)**: DiT가 뽑아낸 특징들을 조합해 최종 스케치와 마스크(Matte)를 출력하는 핵심 모듈 (Base LR 적용)
* **DiT 뒷단 블록 (12 ~ 23) (미세 조정)**: 앞단과 달리 락을 풀고, FPN 디코더가 헤어 특징을 더 잘 찾아낼 수 있도록 아주 조심스럽게 미세 조정 (`LR × 0.1` 배율)
* **DiT 앞단 LoRA (0 ~ 11) (미세 조정)**: 얼어있는 앞단 블록의 Q/K/V 어텐션에 얇은 층(Rank=8)을 덧대어 약간의 유연성만 부여

---

## 2. 순환-교차(Alternating) 학습 플로우 및 구현 디테일

1개의 에폭(Epoch) 내에서 데이터로더가 `shuffle=True` 상태로 무작위 사진 배치를 뽑아낼 때마다, 스텝(`global_step`) 번호에 따라 **두 가지 교육 방식이 번갈아(Alternating)** 실행됩니다. 즉, 짝수와 홀수 스텝에서 사용되는 헤어 사진은 서로 다른 무작위 사진들입니다.

* **짝수 스텝 (Supervised Step)**: 정답지(GT)를 보고 그대로 베끼는 **지도 학습**
* **홀수 스텝 (Cyclic Step)**: 본인이 그린 스케치로 정방향 모델을 통과시켜 원래 헤어를 복원해 보는 **순환 학습**

### 💡 미니 배치 교차(Mini-batch Alternating) 구현 핵심 3요소
한 배치(Batch) 안에서 두 학습을 순차적(Sequential)으로 진행하지 않고 교차 방식으로 분리한 데에는 중요한 설계적 이유가 있습니다.

1. **독립된 목표 집중 (Not Sequential)**
   하나의 배치를 뽑아 `Loss_A`와 `Loss_B`를 연달아 구하지 않고, `if global_step % 2 == 1` 조건문으로 **스텝당 오직 하나의 역할만 수행**하도록 완전히 분리했습니다. 이를 통해 모델이 한 번의 스텝에서 과도하게 두 방향으로 찢어지지 않고 모방(Supervised) 혹은 복원(Cycle)이라는 단일 목표에 집중할 수 있습니다.
2. **마이크로 배치(Micro-batch)를 통한 메모리 폭발 방지**
   `Supervised Step`은 가벼워서 전체 배치를 한 번에 연산할 수 있지만, `Cycle Step`은 거대한 정방향 파이프라인(ControlNet + DiT + VAE + VGG)을 모두 거치므로 심각한 메모리 폭발(OOM)을 유발합니다. 이를 해결하기 위해 Cycle Step 내부에서는 **입력 배치를 잘게 쪼개어(Micro-batch) 연산**하고, 연산 직후 `backward()`를 호출 및 참조 변수를 강제로 삭제(`del`)하여 GPU 메모리를 비워주는 고도의 최적화가 구현되어 있습니다.
3. **얼어있는 모델을 투과하는 Gradient Flow**
   Cycle Step에서는 정방향 생성 모델들(ControlNet, DiT 등)이 얼어있지만(`requires_grad=False`), 코드를 `torch.no_grad()`로 묶지 않고 뚫어두었습니다. 결과적으로 **LPIPS 채점 결과(Gradient)가 얼어있는 모델들을 통로처럼 쭈욱 역류하여 투과**한 뒤, 맨 처음 스케치를 잘못 그렸던 Inverse 모델 파라미터에 정확히 도달하여 가중치를 업데이트하게 됩니다.

---

## 3. 지도 학습(Supervised) vs 순환 학습(Cyclic) 장단점 비교

### 지도 학습 (Supervised Learning)
* **장점 (안정성)**: 정답지가 명확해 학습 초반에 "스케치/마스크의 형태"를 빠르게 잡음. 사람이 의도한 '깔끔하고 정돈된 선'을 그리도록 목줄을 꽉 잡아줌.
* **단점 (한계성)**: 정답지가 엉성하면 모델의 발전도 멈추며(표현의 한계), 정답지가 없는 완전히 새로운 스타일(Domain)이 들어오면 대처 능력이 떨어짐.

### 순환 학습 (Cyclic Learning)
* **장점 (융통성 및 무한 확장)**: "여기에 선을 살짝 추가했더니 머리카락 복원이 더 잘 되네?"를 스스로 깨닫고 미세 질감/잔머리를 추가할 줄 알게 됨. 정답 스케치가 없는 야생의 수만 장 데이터(Unpaired)도 학습에 활용 가능.
* **단점 (꼼수와 불안정성)**: 스케치를 예쁘게 그리기보다 '생성 모델만 알아먹을 수 있는 이상한 노이즈(암호)'를 몰래 숨겨 넣고 복원 점수만 올리려는 꼼수(Shortcut)를 부릴 수 있음. 모델 두 개를 거치므로 메모리를 많이 먹고 Loss 출렁임이 큼.

### 왜 두 방식을 섞어서 쓰는가?
순환 학습이 디테일 복원률을 높이려고 이상한 잡선을 추가하는 꼼수를 부리려 할 때마다, 다음 짝수 스텝에서 지도 학습이 개입해 **"스케치는 사람 눈에도 깔끔해야 해!"**라며 형태를 꽉 잡아줍니다. 
이 팽팽한 줄다리기 덕분에 **"사람이 보기에도 예쁘면서, 헤어 복원도 완벽하게 잘 되는"** 이상적인 스케치가 추출됩니다.

---

## 4. 학습 지표 (Loss) 및 최근 변경 사항

### Total Loss 구성
* **Supervised Step (짝수)**: `(1.0 * Structure) + (0.5 * Color) + (2.0 * Matte) + (0.01 * TV Loss)`
* **Cycle Step (홀수)**: `(0.01 * Feature MSE) + (0.05 * Image LPIPS)`
* 텐서보드의 Total Loss는 스텝마다 위 두 값이 번갈아 찍히므로 매우 크게 출렁이는 것이 정상이며, Epoch Avg Loss는 이 둘의 평균(약 0.4~0.5대)으로 수렴합니다.

### Validation Loss (평가 손실)
* `val_loss`는 훈련 중 쓰이는 꼼수(Cycle, TV Loss 등)를 싹 걷어내고, 오직 **순수 지도 학습 지표(Structure, Color, Matte)**만으로 정답(GT)과 얼마나 똑같은지 아주 깐깐하게 채점한 절대 지표입니다. 

### [0507] 최신 수정 사항: Matte Loss 가중치 강화
* 기존 마스크(Matte) 손실 가중치 `1.0`을 **`2.0`으로 두 배 강화**함. (`configs/inverse_head.yaml`)
* **사유**: 배경 픽셀이 헤어 픽셀보다 압도적으로 많아 발생하는 클래스 불균형 문제를 극복하고, 모델이 얼굴/배경을 헤어로 착각하지 않도록 경계면 분리 능력을 강하게 패널티 주기 위함. (이에 따라 `val_loss` 절대수치도 이전보다 조금 더 높게 찍히며 천천히 하강함)

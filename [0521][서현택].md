# Hair DiT
---

## 방향 재설정

### Bidirectional (Stage 3) 보류
- Bidirectional은 **최후의 카드**로 보류. 수치 개선이 명확할 때만 보조 실험으로 포함

### 현실적 목표

| 우선순위 | 목표 | 방법 |
|---|---|---|
| 1 | Forward 품질 | Edge IoU / Chamfer Distance 개선 (현재 2개 지표 열세) |
| 2 | Inverse 품질 보완 | Stage 2 재학습 (hard brush 출력) |
| 3 | 편집 파이프라인 데모 | Stage 2 inverse → 스케치 수정 → Forward → 새 hair |

---

## Inverse Softbrush 문제 & 해결

### 원인

1. **FPNMaskDecoder**: 32×32 → bilinear×2 × 4회 → 512×512 + sigmoid → 엣지가 필연적으로 smooth
2. **patch_color_sample**: `color_map × stroke_mask` — soft mask와 곱하면 색이 반투명으로 번짐

Forward (Stage 1)은 hard brush 스케치로 학습 → soft brush 입력은 distribution shift → roundtrip 상한선 저하

### 해결: Sharpness Loss 추가

```python
l_sharp = (stroke_mask * (1.0 - stroke_mask)).mean()
# mask가 0 또는 1로 수렴할수록 0 → binary 출력 유도
```

weight `sharp: 0.5` (BCE+Dice와 동일 스케일, 학습 초반 loss_sharp 로그 보며 조정)

### 코드 변경

- `src/training/trainer_inverse_head.py`: `w_sharp` 추가, `_supervised_step`에 l_sharp 계산
- `configs/inverse_stage2.yaml`: `sharp: 0.5` 추가

---

## Stage 2 재학습 설정

```yaml
# configs/inverse_stage2.yaml
loss_weights:
  structure: 1.0
  dice: 1.0
  color: 0.5
  tv: 0.0
  sharp: 0.5      # 신규 추가
  cycle: 0.0
  lpips_cycle: 0.0

checkpointing:
  output_dir: checkpoints/inverse_stage2/
```

```bash
python scripts/train_inverse_head.py --config configs/inverse_stage2.yaml
```

---

## Inverse 모델 원리 요약

```
hair image + matte
  → frozen VAE → hair_latent (16, 64, 64)
  → DiT blocks 0-11 (frozen base + inverse LoRA to_q/k/v rank-8)
    + blocks 12-23 (unfrozen, LR×0.1)
  → learnable agg_weights (3×24) → early/mid/late features (32×32)
  → FPNMaskDecoder → stroke_mask (sigmoid)
  → patch_color_sample(hair×matte, stroke_mask) → sketch_pred
```

**핵심**:
- DiT를 생성이 아닌 **feature extractor**로 재활용 — forward 학습으로 이미 sketch↔hair 표현을 내부에 보유
- LoRA가 Q/K/V를 틀어 "hair → stroke 구조 추출" 방향으로 시선 유도
- 색은 non-parametric — stroke 위치의 원본 hair 색을 그대로 샘플링 (StrokeColorSampler와 동일 원리)

---


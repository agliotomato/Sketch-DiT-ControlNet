# Hair DiT — Stage 1 & Stage 2 기술 정리

> 작성일: 2026-05-12  
> 기준 코드: `main` 브랜치 (커밋 6b13a35 이후 unstaged 수정 포함)

---

## Stage 1 — Hair ControlNet (Forward: Sketch → Hair)

### 목표

Colored sketch + hair matte 조건으로 자연스러운 hair 이미지를 생성하는 forward 방향 diffusion 모델 학습.

### 아키텍처

```
Inputs: sketch (B,3,512,512) + matte (B,1,512,512) + noisy_latent (B,16,64,64)

sketch ──→ frozen VAE.encode ──→ sketch_latent (B,16,64,64) ──┐
matte  ──→ MatteCNN           ──→ matte_feat   (B,16,64,64) ──┤ (+)
                                                               ↓
                                                    ctrl_cond (B,16,64,64)
                                                               ↓
                                            SD3ControlNetModel (trainable)
                                                               ↓
                                                  block_samples (12개)
                                                               ↓
                      SD3.5 DiT (frozen) ← inject residuals ──┘
                                                               ↓
                                                  denoised_latent
                                                               ↓
                                              frozen VAE.decode
                                                               ↓
                                                  hair_image (B,3,512,512)
```

**MatteCNN** — 경량 matte 임베더  
- `(B,1,512,512) → (B,16,64,64)`: stride-2 Conv × 3 (1→16→32→16 ch)  
- sketch_latent에 덧셈 합산 → ControlNet 입력 조건으로 사용  
- Matte-Gated Schedule: matte가 있는 영역에만 hair 생성 집중

**SD3ControlNetModel**  
- SD3.5 Medium transformer에서 초기화 (12개 블록 사용)  
- Null text embedding 2종 학습: `null_encoder_hidden_states (1,333,4096)`, `null_pooled_projections (1,2048)`  
- DiT 본체는 완전 frozen — ControlNet residual만 주입

### 학습 설정

| | Phase 1 (Unbraid pretrain) | Phase 2 (Braid fine-tune) |
|---|---|---|
| Dataset | unbraid_train | braid_train |
| Epochs | 50 | 50 |
| Batch | 2 (grad_accum×4 = eff. 8) | 4 (grad_accum×2 = eff. 8) |
| LR | 1e-4 | 2e-5 |
| Warmup | 500 steps | 100 steps |
| Loss: flow | 1.0 | 1.0 |
| Loss: LPIPS | 0.1 (step 30% 이후 활성) | 0.1 (즉시 활성) |
| Loss: edge | 0.0 | 0.05 |
| Checkpoint | `checkpoints/phase1_unbraid/` | `checkpoints/phase2_braid/` |

**Loss 구성**  
- **Flow Matching (rectified flow)**: `L_FM = MSE(v_pred, v_target)`, `v_target = noise − x₀`  
- **LPIPS**: perceptual loss (VGG backbone), OOM 방지를 위해 warmup 이후 활성  
- **Edge loss**: braid 구조 선명도 보존 (Phase 2에서만 활성)

### 평가 결과

- v2 inference compositor 기준 **9/11 우세** (combined checkpoint epoch 573)  
- Forward 방향 품질 검증 완료 → Stage 2의 frozen ControlNet으로 재사용

---

## Stage 2 — Inverse Model (Hair → Sketch)

### 목표

Hair 이미지와 외부 matte를 입력으로 받아, 해당 hair를 표현하는 colored sketch를 예측하는 inverse 방향 모델 학습.  
Stage 1 ControlNet을 활용한 cycle loss는 Stage 3에서 도입; 이 단계는 순수 supervised warm-start.

### 아키텍처

```
Inputs: hair_image (B,3,512,512) + matte (B,1,512,512)  [GT or SHS/BiSeNet]

hair_image ──→ frozen VAE.encode ──→ hair_latent (B,16,64,64)
                                              ↓
                              DiTFeatureExtractor
                         ┌────────────────────────────────┐
                         │  blocks  0-11  frozen base      │
                         │          +  inverse LoRA        │  (to_q/k/v, rank=8)
                         │  blocks 12-23  back_blocks      │  (fully unfrozen)
                         │                                 │
                         │  hook all 24 blocks →           │
                         │  learnable 3×24 agg weights     │  (softmax over blocks)
                         │  → early / mid / late           │  each (B,1536,32,32)
                         └────────────────────────────────┘
                                              ↓
                                    FPNMaskDecoder
                         ┌────────────────────────────────┐
                         │  proj_late  → 32×32 (256ch)    │
                         │  proj_mid   → merge at 64×64   │
                         │  proj_early → merge at 128×128  │
                         │  → 256 → 512                   │
                         └────────────────────────────────┘
                                              ↓
                                   stroke_mask (B,1,512,512)  [sigmoid]
                                              ↓
                    patch_color_sample(hair×matte, stroke_mask, grid=16)
                                              ↓
                                   sketch_pred (B,3,512,512)
```

### Inverse LoRA 설계

```python
class LoRALinear(nn.Module):
    # out = base(x)  +  (x @ A.T @ B.T) * (alpha / rank)
    # lora_B: zero init  → 학습 초기 LoRA 기여 = 0 (안정적 출발)
    # lora_A: small random init (std=0.01)
```

| 항목 | 값 |
|---|---|
| 적용 위치 | blocks 0-11, to_q / to_k / to_v |
| 모듈 수 | 12 blocks × 3 projections = **36개** |
| rank / alpha | 8 / 8.0 (scale = 1.0) |
| 파라미터 수 | ≈ 885K (학습 가능) |
| base weight | frozen |

**이전 설계(DualLoRALinear)와의 차이**  
이전에는 forward LoRA(fwd_A/B)와 inverse LoRA(inv_A/B)를 한 레이어에 공존시키고 `set_mode()`로 전환했다. forward LoRA가 실제로 FM gradient를 받는 경로에 없었기 때문에 사실상 미사용 파라미터였고, mode switching이 gradient routing을 불안정하게 만들었다. 현재는 **inverse 전용 단일 LoRA**만 두고, FM loss gradient는 back_blocks 전용으로 구조적 분리.

### FPN Mask Decoder

DiT의 모든 블록 출력을 learnable aggregation으로 3개 스케일로 합산 후 FPN 방식으로 디코딩.

```
learnable agg_weights: (3, 24)  →  softmax(dim=1)
  early = Σ w[0,i] * block_i    # texture 담당
  mid   = Σ w[1,i] * block_i    # flow 담당
  late  = Σ w[2,i] * block_i    # semantic 담당

FPN decode:
  late  (256ch, 32×32)   →  ×2 upsample
  mid   (128ch, 64×64)   →  merge (cat → conv)
  early ( 64ch, 128×128) →  merge (×4 upsample + cat → conv)
  → 256×256 → 512×512 → stroke_mask (sigmoid)
```

학습 수렴 후 `get_block_importance()`로 각 스케일별 블록 기여도 시각화 가능.

### 외부 Matte 설계

이전 버전은 모델 내부에 `MatteDecoder`를 두어 hair에서 matte를 예측했다. 이 방식은 matte 품질이 모델에 종속되어 추론 시 SHS Net / BiSeNet과의 입력 분포 차이(domain gap)를 유발한다.  
현재는 **matte를 외부에서 입력**으로 받는 구조로 변경:

| | 이전 | 현재 |
|---|---|---|
| Matte 출처 | 모델 내부 MatteDecoder | 외부 제공 |
| 학습 시 | GT matte로 MatteDecoder 지도 | GT matte를 직접 입력 |
| 추론 시 | MatteDecoder 예측 | SHS Net / BiSeNet 예측 |
| Domain gap | 높음 | 낮음 (추론과 학습 입력 동일 형태) |

### 학습 설정

| 항목 | 값 |
|---|---|
| Epochs | 30 |
| Batch size | 4 (grad_accum×2 = eff. 8) |
| Base LR | 1e-4 |
| back_blocks LR | 1e-5 (backbone_lr_scale = 0.1) |
| Warmup | 200 steps |
| Scheduler | CosineAnnealingLR |
| Mixed precision | bf16 |
| Cycle loss | 비활성 (cycle_start = 9999) |
| ControlNet 로드 | 안 함 (cycle=0 → ~4GB VRAM 절약) |
| Optimizer | AdamW (β=0.9/0.999, wd=1e-2) |

**파라미터 그룹 분리**  
```
main_params  (LoRA + FPN + agg_weights) : lr = 1e-4
back_params  (blocks 12-23)             : lr = 1e-5
```

**Loss 구성**

```
L_total = L_structure + 0.5·L_color + 0.01·L_tv

L_structure = BCE(stroke_mask_pred, stroke_mask_GT)
              where stroke_mask_GT = (sketch_GT.max(ch) > 0.05)

L_color     = L1(sketch_pred, sketch_GT) * stroke_mask_GT / |stroke|
              (stroke 영역에만 가중 평균 → 배경 패널티 없음)

L_tv        = |Δh stroke_mask| + |Δw stroke_mask|
              (마스크 경계 부드럽게 → 선이 끊기지 않도록)
```

### 평가 결과 (Round-trip, braid_test)

> 기준: `checkpoints/inverse_stage2/final.pth` (epoch 30)  
> 평가 방법: hair → HairToSketchDiT → sketch_pred → HairControlNet (Stage 1 final) → hair_recon  
> 지표: hair region (matte masking) 기준, n=30

| 지표 | 평균 | 표준편차 | 방향 |
|------|------|----------|------|
| LPIPS | **0.0732** | ±0.0218 | ↓ 낮을수록 좋음 |
| SSIM  | **0.8925** | ±0.0345 | ↑ 높을수록 좋음 |
| PSNR  | **20.90 dB** | ±3.24 dB | ↑ 높을수록 좋음 |

**해석**

- LPIPS 0.073: 지각적 유사도 매우 높음. 0.1 이하는 사람 눈에 거의 구분 어려운 수준.
- SSIM 0.893: 구조 보존 우수. hair 영역의 streak/curl 방향성이 round-trip 후에도 유지됨.
- PSNR 20.9 dB: 픽셀 수준 재현. braid 같은 복잡한 hair에서 diffusion sampling noise를 감안하면 양호한 수치.

Round-trip이 Stage 1 (frozen) ControlNet을 통하므로, inverse 품질이 실제로는 이보다 높을 수 있음.  
Stage 3 cycle loss 활성 후 동일 기준으로 재측정 예정.

---

### Stage 3 예고

Stage 2 (`checkpoints/inverse_stage2/best.pth`) 결과를 확인한 후 Stage 3 진행.  
Stage 3에서는 `bidirectional=True`로 아래 두 loss를 합산하여 single backward:

```
loss = L_supervised  [→ LoRA + back_blocks]
     + L_fm_forward  [→ back_blocks only, LoRA frozen]
     (+ L_cycle      [epoch >= cycle_start 이후])
```

- **back_blocks**: 양방향에서 gradient를 받아 forward/inverse 공유 특징 학습
- **inverse LoRA**: inverse supervised gradient만 수신 (FM step 시 freeze)
- **LR**: 5e-5 (Stage 2 대비 절반 — refinement 단계)

---

## Stage 3 중간 결과 (Epoch 40, Cycle 활성 전)

> 기준: `checkpoints/inverse_stage3/last.pth` (epoch 40)  
> 평가 방법: Stage 2와 동일 (Stage 1 ControlNet final 고정 사용)  
> n=50 (Stage 2 대비 샘플 수 증가)

| 지표 | Stage 2 (ep30, n=30) | Stage 3 (ep40, n=50) | 변화 |
|------|----------------------|----------------------|------|
| LPIPS | 0.0732 ± 0.0218 | 0.0821 ± 0.0290 | +0.009 ↑ (소폭 악화) |
| SSIM  | 0.8925 ± 0.0345 | 0.8800 ± 0.0437 | -0.013 ↓ (소폭 악화) |
| PSNR  | 20.90 ± 3.24 dB | 19.91 ± 3.61 dB | -1.0 dB ↓ (소폭 악화) |

**학습 곡선 (epoch 31–40, cycle 활성 전)**

| Epoch | avg loss (train) | val_loss_inv | val_loss_fwd |
|-------|-----------------|-------------|-------------|
| 31    | 0.3320          | —           | —           |
| 32    | 0.2460          | 0.8429      | 0.9692      |
| 33    | 0.2097          | —           | —           |
| 34    | 0.2009          | 0.8657      | 0.8829      |
| 35    | 0.1948          | —           | —           |
| 36    | 0.1910          | 0.8751      | 0.8448      |
| 37    | 0.1918          | —           | —           |
| 38    | 0.1928          | 0.8611      | 0.8120      |
| 39    | 0.1960          | —           | —           |
| 40    | 0.1973          | 0.8618      | 0.8146      |

- **avg loss**: 0.33 → 0.19 수렴 (FM + supervised 합산)
- **val_loss_inv**: 0.843 수준에서 안정 — supervised inverse 품질 유지
- **val_loss_fwd**: 0.969 → 0.812 지속 하락 — joint training으로 ControlNet도 개선

**해석**

Stage 3 epoch 40은 **cycle loss 활성 전** (cycle_start=40) 상태로, FM forward loss만 추가된 시점이다.  
수치가 소폭 하락한 원인은 두 가지로 분석된다:

1. **back_blocks가 FM gradient를 받기 시작**: 공유 backbone이 forward (sketch→hair) 방향에도 최적화되면서 inverse 특화도가 일시적으로 희석됨. Cycle loss 활성 후 inverse 방향 피드백이 강화되면 회복 예상.

2. **샘플 수 차이 (n=30 → n=50)**: 더 어려운 샘플이 포함되어 평균이 낮아진 효과 포함.

`val_loss_fwd` 추세는 양호 (0.969 → 0.815 / epoch 32→40) — ControlNet 품질이 joint training으로 오히려 개선되고 있음.

Epoch 41부터 cycle loss (feature MSE) 활성 후 재측정 예정.

---

## 파일 구조 요약

```
configs/
  phase1_unbraid.yaml      # Stage 1 Phase 1
  phase2_braid.yaml        # Stage 1 Phase 2
  inverse_head.yaml        # Stage 2/3 base config
  inverse_stage2.yaml      # Stage 2 override
  inverse_stage3.yaml      # Stage 3 override

src/models/
  controlnet_sd35.py       # HairControlNet (Stage 1)
  dit_feature_extractor.py # DiTFeatureExtractor + LoRALinear
  inverse_head.py          # HairToSketchDiT + FPNMaskDecoder

src/training/
  trainer.py               # Stage 1 trainer
  trainer_inverse_head.py  # Stage 2/3 trainer

checkpoints/
  phase2_braid/final.pth   # Stage 1 최종 체크포인트
  inverse_stage2/best.pth  # Stage 2 출력 → Stage 3 resume
```

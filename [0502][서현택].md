# Hair-DiT: Sketch-Guided Hair Synthesis — 파이프라인 정리

> 코드베이스 기준: `hair-dit`, checkpoint `checkpoints/phase2_braid/final.pth`

---

## 0. 전체 파이프라인 개요

```
[입력]
  Sketch  (B, 3, 512, 512)  ─┐
  Matte   (B, 1, 512, 512)  ─┤─→ HairControlNet ─→ block_samples (12개 residual)
  Noisy latent (B,16,64,64) ─┘         ↓
                                 SD3.5 Transformer (frozen)
                                         ↓
                                   v_pred  (velocity)
                                         ↓
                              Flow Matching Euler Step ×20
                                         ↓
                                hair_latent (B,16,64,64)
                                         ↓
                                   VAE Decode
                                         ↓
                                hair_patch (B,3,512,512)
                                         ↓
[face 이미지 있을 때]        Latent + Pixel 2단계 합성 (v4)
                                         ↓
                                final_image (B,3,512,512)
```

---

## 1. 모델 구성 요소

| 컴포넌트 | 역할 | 학습 여부 |
|---------|------|----------|
| `VAEWrapper` | 이미지 ↔ latent 변환 (SD3.5 VAE) | Frozen |
| `MatteCNN` | matte → 16ch latent feature | **Trainable** |
| `HairControlNet` (= SD3ControlNetModel) | sketch+matte conditioning → block residuals | **Trainable** |
| `null_encoder_hidden_states` | 학습된 null text embedding `(1,333,4096)` | **Trainable** |
| `null_pooled_projections` | 학습된 null pooled embedding `(1,2048)` | **Trainable** |
| `SD3Transformer2DModel` | denoising backbone (SD3.5 medium) | Frozen |
| `FlowMatchEulerDiscreteScheduler` | timestep/sigma 관리 | N/A |

---

## 2. Hair Patch 생성: Control Conditioning (`ctrl_cond`) 구성

HairControlNet의 핵심은 sketch와 matte를 latent 해상도(64×64)에서 하나의 conditioning tensor로 융합하는 것이다.

### 2-1. MatteCNN: Matte → Latent Feature

matte는 헤어 영역 마스크이므로 공간적 구조가 중요하다. 단순 bilinear downsample 대신 학습 가능한 CNN으로 latent space에 맞는 feature로 변환한다.

```
Input:  (B,  1, 512, 512)  — 헤어 영역 마스크 [0,1]

Conv(1→16,  k=3, s=2, p=1) + GroupNorm(4,16)  + SiLU  →  (B, 16, 256, 256)
Conv(16→32, k=3, s=2, p=1) + GroupNorm(8,32)  + SiLU  →  (B, 32, 128, 128)
Conv(32→16, k=3, s=2, p=1) + GroupNorm(4,16)  + SiLU  →  (B, 16,  64,  64)

Output: matte_feat (B, 16, 64, 64)
```

stride-2 convolution을 3번 적용해 512→64(8× downsample). VAE latent와 동일한 공간 해상도. bias=False (GroupNorm이 편향 흡수).

### 2-2. Sketch Encoding: Frozen VAE Encoder

스케치 이미지를 VAE encoder로 latent 표현으로 변환한다. VAE는 frozen이므로 gradient가 흐르지 않는다.

```
Input:  sketch  (B, 3, 512, 512)  [0,1]
  → *2 - 1  →  [-1,1]  (VAE 입력 정규화)
  → VAE encode  →  posterior.sample()
  → (x - shift) * scale   (shift=0.0609, scale=1.5305)

Output: sketch_latent  (B, 16, 64, 64)
```

**왜 sketch를 VAE로 인코딩하는가?**
스케치는 sparse한 갈색 선으로 구성되어 있어 pixel space보다 VAE latent space에서 더 컴팩트하고 의미있는 표현을 가진다. 또한 SD3.5 Transformer는 latent space에서 동작하므로, conditioning도 동일한 표현 공간에 있어야 한다.

### 2-3. `ctrl_cond` 조합 (17ch)

```python
# Step 1: sketch + matte feature → element-wise 합산
ctrl_cond_16 = sketch_latent + matte_feat            # (B, 16, 64, 64)

# Step 2: raw matte를 64×64로 downsample → 17번째 채널로 explicit 공간 정보 추가
matte_64 = F.interpolate(matte, size=(64,64), mode='bilinear', align_corners=False)
                                                      # (B,  1, 64, 64)

# Step 3: concat → SD3ControlNetModel에 전달
ctrl_cond = torch.cat([ctrl_cond_16, matte_64], dim=1)  # (B, 17, 64, 64)
```

**설계 의도:**
- `sketch_latent + matte_feat`: 두 조건을 element-wise 합산하여 공간적으로 정렬된 단일 feature로 융합. Concatenation 대신 addition을 쓰는 이유는 SD3ControlNetModel의 `extra_conditioning_channels=1` API 제약 (16ch baseline + 1ch extra).
- `matte_64` (17번째 채널): raw matte를 별도 채널로 제공하여 SD3ControlNet이 명시적인 헤어 영역 경계 정보를 직접 참조할 수 있게 한다.

### 2-4. Null Text Embedding

SD3.5는 텍스트 조건을 필요로 하지만, 본 모델은 텍스트 없이 sketch+matte만으로 조건화한다. 학습 가능한 null embedding을 사용한다.

```python
null_enc_hs   = self.null_encoder_hidden_states.expand(B, -1, -1)  # (B, 333, 4096)
null_pooled   = self.null_pooled_projections.expand(B, -1)         # (B, 2048)
```

두 embedding은 `nn.Parameter`로 학습되며, 초기값은 0. 학습을 통해 "텍스트 없이 sketch/matte만으로 생성"하는 데 최적화된 null conditioning을 학습한다.

### 2-5. HairControlNet Forward 전체

```python
block_samples = controlnet(
    hidden_states   = noisy_latent,    # (B, 16, 64, 64)
    controlnet_cond = ctrl_cond,       # (B, 17, 64, 64)  ← 위에서 구성
    encoder_hidden_states = null_enc_hs,  # (B, 333, 4096)
    pooled_projections    = null_pooled,  # (B, 2048)
    timestep = sigma,                  # (B,)  flow matching sigma
    return_dict = False,
)[0]
# block_samples: list of 12 residual tensors (SD3.5 transformer block별 residual)
```

`block_samples`는 SD3.5 Transformer의 각 transformer block에 더해지는 residual로, ControlNet이 denoising에 개입하는 방식이다.

---

## 3. Flow Matching Sampling Loop (Inference)

### 3-1. 이론적 배경

Flow Matching은 노이즈 분포 `p(z) = N(0,I)`와 데이터 분포 `p(x)` 사이를 선형 경로로 연결한다:

```
x_t = (1 - σ_t) * x_0 + σ_t * ε,   ε ~ N(0,I)
```

모델은 velocity `v = ε - x_0`를 예측하고, 이를 이용해 Euler 방법으로 `x_0`로 적분한다.

**Logit-normal sigma sampling (학습 시):**
`σ ~ sigmoid(N(0,1))`로 샘플링하여 중간 timestep에 더 많은 가중치를 둔다.

### 3-2. Sampling 알고리즘

```python
scheduler.set_timesteps(num_steps=20, device=device)
latents = torch.randn(1, 16, 64, 64, device=device, dtype=bfloat16)  # x_T ~ N(0,I)

for i, t in enumerate(scheduler.timesteps):   # t: T → 0
    σ = scheduler.sigmas[i]                    # (1,)  현재 노이즈 레벨

    # 1. ControlNet: ctrl_cond에서 block residuals 생성
    block_samples, null_enc_hs, null_pooled = HairControlNet(
        noisy_latent=latents, sketch=sketch, matte=matte, sigmas=σ
    )

    # 2. Transformer: velocity prediction
    v_pred = SD3Transformer(
        hidden_states                  = latents,
        encoder_hidden_states          = null_enc_hs,
        pooled_projections             = null_pooled,
        timestep                       = σ,
        block_controlnet_hidden_states = block_samples,
    )  # (1, 16, 64, 64)

    # 3. Euler step: x_{t-1} = x_t - σ_t * v_pred
    latents = scheduler.step(v_pred, t, latents).prev_sample
```

20 step으로 약 14분 소요 (GPU에 따라 다름). step당 ControlNet + Transformer 각 1 forward pass.

### 3-3. Hair Patch Decode

```python
hair_patch = (vae.decode(latents).clamp(-1, 1) + 1) / 2  # [0,1], (1,3,512,512)
```

VAE decode:
```
latent → latent / 1.5305 + 0.0609   (역정규화)
       → AutoencoderKL decode
       → (image + 1) / 2             → [0,1]
```

---

## 4. Face + Hair Patch 합성 (Compositing)

Hair patch와 원본 face 이미지를 matte를 이용해 합성한다. 4가지 방식(v1–v4)을 실험하였고 v4가 최종 채택되었다.

### 4-1. 공통 사전 처리

```python
face_latent  = vae.encode(face)           # (1, 16, 64, 64)
matte_64     = F.interpolate(matte, (64,64), mode='area')   # (1,  1, 64, 64)
#  mode='area': 픽셀 평균 보존 → soft matte boundary를 latent에서도 유지
```

**area pooling 선택 이유**: 헤어 경계부의 반투명 matte 값(0~1 사이)을 downsample할 때 nearest/bilinear보다 area pooling이 평균값을 더 잘 보존한다. 경계 픽셀의 soft transition이 latent 합성 시 자연스러운 블렌딩으로 이어진다.

### 4-2. 버전별 합성 비교

| 버전 | Latent 합성 방식 | Pixel 후처리 | 특징 |
|------|----------------|-------------|------|
| **v1** | `hair_latent * (m>0.5) + face_latent * (m≤0.5)` | hard mask: face 픽셀로 교체 | 경계 aliasing 심함 |
| **v2** | `hair_latent * m + face_latent * (1-m)` | 없음 | VAE decode 아티팩트 잔존 |
| **v3** | `hair_latent * m + face_latent * (1-m)` | hard mask: face 픽셀로 교체 | sketch fidelity↑, boundary 거침 |
| **v4** | `hair_latent * m + face_latent * (1-m)` | `decoded * m + face * (1-m)` (soft) | **전체 최고** |

### 4-3. v4 상세: 2단계 Soft Blending

```python
# [Stage 1] Latent space soft blend
composite_latent = hair_latent * matte_64 + face_latent * (1.0 - matte_64)
                                                          # (1, 16, 64, 64)

# [Stage 2] VAE decode
decoded = (vae.decode(composite_latent).clamp(-1,1) + 1) / 2   # (1, 3, 512, 512) [0,1]

# [Stage 3] Pixel space soft blend (post-processing)
final = decoded * matte_512 + face * (1.0 - matte_512)          # (1, 3, 512, 512) [0,1]
# matte_512: 원본 해상도 512×512 matte 사용
```

**각 단계의 역할:**

| 단계 | 목적 |
|------|------|
| Stage 1 (latent soft blend) | hair latent와 face latent를 matte 비율로 혼합하여 경계부 latent를 부드럽게 전환 |
| Stage 2 (VAE decode) | 혼합된 latent에서 pixel image 복원. 경계부 텍스처가 자연스럽게 보간됨 |
| Stage 3 (pixel soft blend) | VAE decode 과정의 reconstruction artifact 보정 + face 영역 원본 픽셀 보존 |

**v4가 v3보다 좋은 이유**: Stage 3에서 hard mask 대신 soft blend를 쓰면 경계부 픽셀이 양쪽 이미지의 가중 평균으로 결정되므로 sharp seam이 생기지 않는다. Boundary FID가 39.07 → 21.97로 대폭 개선 (↓43.8%).

---

## 5. 학습 전략

### 5-1. 2단계 커리큘럼

| 단계 | 데이터 | LR | Epochs | Loss |
|------|--------|-----|--------|------|
| Phase 1 (Pretrain) | unbraid ~3K | 1e-4 | 50 | L_flow + L_lpips (30% 이후) |
| Phase 2 (Finetune) | braid ~1K | 2e-5 | 50 | L_flow + L_lpips + L_edge |

Phase 1에서 일반적인 헤어 합성 능력을 확보하고, Phase 2에서 복잡한 braid 구조에 특화한다.

### 5-2. Loss 함수

**총 Loss:**
```
L = 1.0 × L_flow  +  0.1 × L_lpips  +  0.05 × L_edge   (Phase 2)
L = 1.0 × L_flow  +  0.1 × L_lpips                      (Phase 1, warmup 이후)
```

**L_flow (Flow Matching Loss, matte-weighted MSE):**
```
L_flow = mean( w * (v_pred - v_target)² )
w = matte_latent + 0.1 * (1 - matte_latent)   # 헤어 영역 1.0, 배경 0.1
v_target = ε - x_0                             # flow matching velocity target
```

**L_lpips (Perceptual Loss):**
- 예측된 x_0 = x_t - σ_t * v_pred 에서 VAE decode하여 pixel image 복원
- matte 영역 내에서만 LPIPS 측정

**L_edge (Sketch Edge Alignment, Phase 2 전용):**
- 스케치 stroke 위치에서 생성 이미지의 Sobel edge magnitude가 낮으면 패널티
- braid fine structure의 구조적 충실도 향상 목적

---

## 6. 정량 평가 결과 요약 (n=107 test samples)

| 지표 | GAN (SHS) | DiT v1 | DiT v2 | DiT v3 | **DiT v4 (Ours)** |
|------|:---------:|:------:|:------:|:------:|:-----------------:|
| Hair FID ↓ | 195.16 | 177.72 | 174.09 | 171.23 | **170.82** |
| LPIPS (GT) ↓ | 0.3263 | 0.3250 | 0.3254 | 0.3227 | **0.3209** |
| SSIM ↑ | 0.5906 | 0.5936 | 0.6012 | 0.6011 | **0.6073** |
| PSNR ↑ | 11.18 | 12.16 | 12.40 | 12.41 | **12.41** |
| Boundary FID ↓ | 50.97 | 37.17 | 40.89 | 39.07 | **21.97** |
| Boundary LPIPS ↓ | 0.0272 | 0.0267 | 0.0361 | 0.0355 | **0.0152** |
| Face LPIPS ↓ | 0.0036 | **0.0000** | 0.0302 | 0.0301 | 0.0015 |
| ArcFace Cos ↑ | 0.7696 | 0.7916 | 0.7781 | 0.7797 | **0.7981** |
| Edge IoU ↑ | 0.1031 | 0.1047 | 0.1052 | **0.1062** | 0.1052 |
| Chamfer Dist ↓ | 2.6926 | 2.6739 | 2.6636 | **2.6412** | 2.6474 |

**v4 핵심 강점**: Hair 생성 품질(FID, LPIPS, SSIM), Boundary 자연스러움(Boundary FID ↓56.9%), Face identity 보존(ArcFace Cos)에서 동시에 최고 성능.

**v4의 유일한 약점**: Edge IoU, Chamfer Distance에서 v3 대비 소폭 열세 (pixel-level 경계 hard replace 포기에 따른 trade-off).

> 전체 per-sample 결과 및 best/worst 20 examples → `[0430][서현택].md` 참조.

---

## 7. Limitation

### 7-1. 복잡한 레이어링 구조 (Multi-layer Hair)
다수의 wisp strand로 형성된 복잡한 레이어 헤어스타일은 깊이 정보 없이 2D 스케치만으로 복원 불가. Strand 간 depth 관계가 스케치에서 손실되므로 layering 구조 재현 한계.

### 7-2. 특수 헤어스타일: 드레드락 / 스파이럴 / 코일
자기 폐색(self-occluded) wisp나 코일 구조를 단일 stroke로 표현하면, 모델이 학습 데이터 분포(wavy hair)로 회귀. 스케치로 이런 구조를 명확히 표현하기 어렵고, 데이터셋 부족도 원인.

### 7-3. 조명 불일치
생성된 hair 영역과 배경 간 lighting condition 불일치로 경계 아티팩트 발생. Hair foreground relighting은 현재 미해결 과제(open problem)로 파이프라인 내 처리 불가.

---

## 8. 외부 스케치 테스트 (Generalization)

학습 데이터 외 스케치(nanobanana로 생성, 워터마크 포함)로 generalization 테스트.

**전처리 (`scripts/clean_sketch.py`)**:
- Sketch: 배경 강제 0, 스트록 단일 갈색(180,120,50), 우측 하단 워터마크 제거 (threshold=80, 50×50 corner black)
- Matte: threshold=128 이진화, 워터마크 제거

| | GT | Sketch (정제 후) | Matte (정제 후) | 생성 결과 (v4) |
|:--:|:--:|:---------------:|:---------------:|:--------------:|
| limit 1 | ![](dataset/braid/img/test/limit_GT.png) | ![](dataset/braid/sketch/test/limit_sketch_clean.png) | ![](dataset/braid/matte/test/limit_matte_clean.png) | ![](custom_results/dit/watermark2/limit_sketch_clean_full.png) |
| limit 2 | ![](dataset/braid/img/test/limit2_GT.png) | ![](dataset/braid/sketch/test/limit2_sketch_clean.png) | ![](dataset/braid/matte/test/limit2_matte_clean.png) | ![](custom_results/dit/watermark2/limit2_sketch_clean_full.png) |

# SemiSpatialNet 학습 원리 정리 (0528)

질문 답변

1. inverse 는 backbone 공유에. lora 별도로?

- 아닙니다. backbone 공유는 안합니다. Forward와 inverse는 완전히 별개의 모델입니다. 

- Forward : 이미 학습된 ControlNet
- Inverse : SD3.5 DiT를 사용. 여기에 inverse 전용 LoRA(rank = 8) 부착

2. 학습은 bidirectional 인지 아닌지?

- 단방향입니다. forward, inverse 독립적으로 학습되었습니다.

3. Roundtrip 이 의미하는 것은 cyclic?

- GT_hair에서 inverse 모델이 뽑은 sketch를 forward ControlNet 에 입력하였을 때 생성한 결과를 RoundTrip이라고 하였습니다. 

## 1. 전체 목표

**Hair Photo → Colored Sketch** 변환 (Inverse 방향)

Forward 모델 (Sketch → Hair) 이 이미 학습돼 있고,  
Inverse 모델은 그 반대 방향을 학습한다.

생성된 Sketch는 Forward ControlNet의 입력으로 사용된다.

```
[실제 헤어 사진] ──[Inverse]──▶ [스케치] ──[Forward ControlNet]──▶ [재생성된 헤어 사진]
```

---

## 2. GT(Ground Truth) 스케치가 뭔가

데이터셋에 `dataset/{style}/sketch/{split}/*.png` 형태로 저장된 **실제 파일**이다.

- 각 헤어 가닥(strand)마다 고유한 색상으로 구분된 stroke map
- 학습 시 `StrokeColorSampler`가 그 고유색을 **실제 헤어 사진의 해당 위치 픽셀 색**으로 교체
  - 67% 확률: 해당 strand 영역 픽셀의 평균색
  - 33% 확률: 해당 strand 영역에서 랜덤 픽셀 1개 색
- 결과: stroke 위치는 원본, 색은 실제 헤어 색

```
원본 스케치 파일           StrokeColorSampler 적용 후
(파란색 strand A)    →    (갈색 #7a4a2b — 실제 헤어 색)
(빨간색 strand B)    →    (금발 #d4a05c — 실제 헤어 색)
(초록색 strand C)    →    (검은색 #1a1a1a — 실제 헤어 색)
```

---

## 3. 기존 모델이 왜 실패했나

### `HairToSketchDiT` (이전 6번 시도)

```
hair → VAE.encode → latent(64×64)
     → DiT features → {early, mid, late} (모두 32×32)
     → FPN decoder → binary stroke_mask (1채널)
     → patch_color_sample(grid=16×16) → sketch_pred
```

**실패 원인 3가지:**

| 문제 | 설명 |
|------|------|
| 32×32 feature | strand 1개가 1~3px인데 32×32에선 위치 정보 소실 |
| binary mask | GT는 RGB per-strand 구조인데 1채널 mask로 표현 불가 |
| 16×16 grid color | 32px 블록 평균색 → 개별 strand 색 구분 불가, 전부 평균 갈색 |

---

## 4. SemiSpatialNet 아키텍처

### 4-1. 전체 데이터 흐름

```
Input: hair_image (B, 3, 512, 512)
       matte      (B, 1, 512, 512)   ← GT matte (학습 시), 추론 시엔 외부 모델

                    ┌─────────────────────────────────────────┐
                    │           SemiSpatialNet                │
                    │                                         │
  [hair + matte]   │  ┌──────────────┐   ┌─────────────────┐│
  (B, 4, 512, 512) │  │  CNN Encoder │   │ DiTFeatureExtrac││
        │           │  │  (pixel sp.) │   │ tor (latent sp.)││
        ▼           │  └──────┬───────┘   └────────┬────────┘│
                    │         │ skip×3              │ 'late'  │
                    │         ▼                     ▼         │
                    │  ┌──────────────────────────────────┐   │
                    │  │   CrossAttentionFusion           │   │
                    │  │   Q=CNN L4 / K,V=DiT late        │   │
                    │  └──────────────┬───────────────────┘   │
                    │                 │ fused (B,512,64,64)    │
                    │                 ▼                        │
                    │  ┌──────────────────────────────────┐   │
                    │  │   UNetDecoder (skip connections) │   │
                    │  └──────────────┬───────────────────┘   │
                    └─────────────────┼───────────────────────┘
                                      │
                    ┌─────────────────┴──────────────────┐
                    ▼                                     ▼
             sketch_pred                           stroke_mask
           (B, 3, 512, 512)                      (B, 1, 512, 512)
             [주 출력: RGB]                        [보조 출력: binary]
```

---

### 4-2. Path 1 — CNN Encoder (pixel space)

**입력**: `[hair(3ch) + matte(1ch)]` concat → `(B, 4, 512, 512)`

| Layer | 연산 | 출력 shape | 역할 |
|-------|------|------------|------|
| L1 | Conv(4→64, stride=1) × 2 | `(B, 64, 512, 512)` | 텍스처 detail, skip→decoder |
| L2 | Conv(64→128, stride=2) × 2 | `(B, 128, 256, 256)` | 중간 구조, skip→decoder |
| L3 | Conv(128→256, stride=2) × 2 | `(B, 256, 128, 128)` | stroke 패턴, skip→decoder |
| L4 | Conv(256→512, stride=2) × 2 | `(B, 512, 64, 64)` | bottleneck, Cross-Attn Q |

각 레벨은 `Conv2d + GroupNorm + SiLU` 2개 쌓은 구조.  
**핵심**: DiT와 달리 512→64px까지 공간 정보를 단계적으로 보존하면서 내려간다.

---

### 4-3. Path 2 — DiTFeatureExtractor (latent space)

**입력**: `hair_image` → `VAE.encode()` → `hair_latent (B, 16, 64, 64)` (frozen VAE)

SD3.5-medium DiT를 통해 semantic feature 추출:
- **Blocks 0-11**: frozen base + inverse LoRA (rank=8, to_q/to_k/to_v에 적용)
- **Blocks 12-23**: `back_blocks` — fully unfrozen, 낮은 LR(×0.1)로 학습
- 24개 블록에 hook → 각 블록의 image token (1024개, 32×32) 캡처
- learnable aggregation weights (3×24 softmax) → `early`, `mid`, `late` feature map 생성
- 각 feature map shape: `(B, 1536, 32, 32)`

**Cross-Attention에는 `late` feature만 사용** (가장 semantic한 representation)

---

### 4-4. CrossAttentionFusion (핵심 fusion)

**역할**: CNN의 공간 정밀도 + DiT의 의미 정보를 결합

```
Q  = CNN L4   (B, 512, 64, 64)
     → avg_pool2d(kernel=2) → (B, 512, 32, 32)   # 메모리 절감: 4096→1024 토큰
     → flatten → (B, 1024, 512)
     → Linear(512→512) → (B, 1024, 512)

K,V = DiT late (B, 1536, 32, 32)
     → flatten → (B, 1024, 1536)
     → Linear(1536→512) → (B, 1024, 512)

MultiheadAttention(d=512, nhead=8):
  각 헤드 d_head = 64
  attention matrix: (B, 8, 1024, 1024) ≈ 100MB@bf16  → 메모리 안전

output: (B, 1024, 512) → reshape (B, 512, 32, 32)
        → bilinear upsample → (B, 512, 64, 64)

fused = L4 + attn_out   (residual connection)
```

**직관**: CNN의 각 공간 위치(strand가 있는 곳)가 DiT의 semantic 토큰에게  
"이 위치에서 어떤 head 방향/색이 적합한가?" 라고 쿼리한다.

---

### 4-5. UNetDecoder (skip connections)

bottleneck `fused (B, 512, 64, 64)` 에서 `512×512`로 복원.  
CNN encoder의 skip connection을 concat해서 고해상도 detail 복원.

```
fused (B, 512, 64, 64)
  → upsample ×2 → cat(L3 skip: 256ch) → Conv → (B, 256, 128, 128)
  → upsample ×2 → cat(L2 skip: 128ch) → Conv → (B, 128, 256, 256)
  → upsample ×2 → cat(L1 skip:  64ch) → Conv → (B,  64, 512, 512)
```

여기서 두 output head 분기:

```python
sketch_head: Conv(64→3) + Sigmoid  →  sketch_pred (B, 3, 512, 512)
mask_head:   Conv(64→1) + Sigmoid  →  stroke_mask (B, 1, 512, 512)
```

---

## 5. Loss 구성

### 5-1. sketch_pred 관련 (주 학습 신호)

#### Color L1 Loss (masked)
```python
stroke_mask_gt = (sketch_gt.max(dim=1) > 0.05).float()  # stroke가 있는 픽셀
l_color = L1(sketch_pred, sketch_gt) * stroke_mask_gt   # stroke 영역에서만
```
- stroke가 있는 픽셀에서만 색 예측 정확도를 평가
- 빈 배경(검은색)은 무시

#### Perceptual Loss (LPIPS VGG)
```python
pred_11 = sketch_pred * 2 - 1   # [-1, 1]로 변환
gt_11   = sketch_gt * 2 - 1
# 256×256으로 downsample 후 VGG feature 비교
l_perc = lpips_fn(downsample(pred_11), downsample(gt_11))
```
- L1만 쓰면 여러 strand 색의 평균(갈색 blob)이 loss 최솟값이 됨
- Perceptual loss는 VGG feature 공간에서 비교해 **구조적 유사성** 강제
- 이게 핵심: mean-collapse 방지

### 5-2. stroke_mask 관련 (보조 학습 신호)

#### BCE (Binary Cross-Entropy)
```python
stroke_mask_gt = (sketch_gt.max(dim=1) > 0.05).float()
l_structure = BCE(stroke_mask, stroke_mask_gt)
```
- stroke_mask 보조 head가 "stroke가 있는 위치"를 올바르게 예측하도록 유도

#### Dice Loss
```python
l_dice = 1 - (2 * intersection + 1) / (pred.sum() + gt.sum() + 1)
```
- class imbalance 보정 (배경이 훨씬 많음)

### 5-3. 최종 Loss 합산

```
total = w_structure × BCE + w_dice × Dice + w_color × L1_masked + w_perceptual × LPIPS

현재 config (inverse_semi_spatial.yaml):
  w_structure  = 1.0
  w_dice       = 1.0
  w_color      = 0.5
  w_perceptual = 0.1
  w_tv         = 0.0  (비활성)
  w_sharp      = 0.0  (비활성)
```

---

## 6. Optimizer 구조

| Param group | 대상 | LR |
|-------------|------|----|
| main | CNN encoder, cross_attn, decoder, LoRA | 1e-4 |
| backbone | DiT back_blocks (12-23) | 1e-5 (×0.1) |

- Warmup 200 step → CosineAnnealingLR
- AdamW (β=0.9/0.999, wd=1e-2)
- gradient clip 1.0

---

## 7. 학습 Phase

현재 config (`inverse_semi_spatial.yaml`): **supervised-only**

```
cycle_start = 9999  →  cycle loss 비활성

Phase 1 (전체):
  매 step: _supervised_step()
    → L1 + Perceptual + BCE + Dice
```

나중에 cycle loss 붙이려면 `cycle_start`를 낮추면 됨:
```yaml
cycle_start: 20   # epoch 20부터 cycle loss 활성화
cycle: 0.01
lpips_cycle: 0.05
```

---

## 8. Input / Output 정리

| | Shape | 값 범위 | 설명 |
|-|-------|---------|------|
| **Input: hair_image** | `(B, 3, 512, 512)` | [0, 1] | 원본 헤어 사진 |
| **Input: matte** | `(B, 1, 512, 512)` | [0, 1] | 헤어 영역 마스크 (학습: GT, 추론: SHS/BiSeNet) |
| **Output: sketch_pred** | `(B, 3, 512, 512)` | [0, 1] | 예측 스케치 (RGB, 주 출력) |
| **Output: stroke_mask** | `(B, 1, 512, 512)` | [0, 1] | 이진 stroke mask (보조 출력) |
| **GT: sketch** | `(B, 3, 512, 512)` | [0, 1] | StrokeColorSampler 적용 후 |
| **GT: matte** | `(B, 1, 512, 512)` | [0, 1] | 헤어 영역 정답 |

---

## 9. 기존 대비 달라진 점 요약

| | HairToSketchDiT (실패) | SemiSpatialNet (신규) |
|-|------------------------|----------------------|
| Spatial resolution | DiT feature 32×32 | CNN pixel-space 512→64 |
| 출력 표현 | binary mask + grid color | 직접 RGB prediction |
| Color 방식 | 16×16 grid avg pool | end-to-end learned |
| 핵심 loss | L1 (mean-collapse 유발) | L1 + LPIPS perceptual |
| DiT 역할 | 주 feature extractor | semantic 보조 (K, V) |
| CNN 역할 | 없음 | 주 spatial encoder (Q) |

---

## 10. 학습 실행

```bash
accelerate launch scripts/train_inverse_head.py \
  --config configs/inverse_semi_spatial.yaml
```

평가 (roundtrip):
```bash
python scripts/eval_roundtrip.py \
  --inverse_ckpt checkpoints/inverse_semi_spatial/best.pth \
  --inverse_config configs/inverse_semi_spatial.yaml \
  --forward_ckpt checkpoints/phase2_braid/final.pth \
  --forward_config configs/phase2_braid.yaml \
  --split braid_test --num_samples 50
```
